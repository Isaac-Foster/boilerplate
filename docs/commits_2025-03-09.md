# Commits — 2025-03-09

**Data:** 2025-03-09

## Tabela de commits

| hash_id | file | message |
|---------|------|---------|
| 80b6e84 | {{ cookiecutter.project_slug }}/config/__init__.py | refactor(config): load config from APP_CONFIG json or env with _clean for quoted strings |
| 55bc666 | {{ cookiecutter.project_slug }}/pyproject.toml | chore: update dev deps and pytest/ruff config, add task start |
| 8a0d338 | {{ cookiecutter.project_slug }}/Dockerfile | chore: add Dockerfile with uv and python 3.12-slim |
| 664ef03 | {{ cookiecutter.project_slug }}/docker-compose.yaml | chore: add docker-compose for project service |
| a95c8d9 | {{ cookiecutter.project_slug }}/extras/README.md | docs: add extras README with New Relic health-check filter snippet |
| f43555f | {{ cookiecutter.project_slug }}/newrelic.ini | chore: add New Relic Python agent config template |
| c866b52 | docs/commits_2025-03-09.md | docs: add commits log for 2025-03-09 |

## Resumo das mudanças

- **config/__init__.py:** Config passou a ser carregada a partir da variável de ambiente `APP_CONFIG` (JSON) ou de `os.environ`; adicionada função `_clean` para remover aspas em valores string; desativado carregamento de `.env` e logging em arquivo (logs só no formato configurado).

- **pyproject.toml:** Atualizadas dependências de desenvolvimento (ruff 0.12.11, removidos pydantic-settings e pytest-asyncio do grupo dev); simplificadas opções do pytest (asyncio_mode, testpaths); lint do ruff passou a usar `--fix`; adicionada task `start` no taskipy.

- **Dockerfile:** Novo Dockerfile baseado em python:3.12-slim com uv para instalação de dependências; copia `pyproject.toml`, `uv.lock`, `app/` e `config/`; CMD com `uv run task start`.

- **docker-compose.yaml:** Novo docker-compose com um serviço que faz build da imagem, usa `.env`, define variáveis de ambiente e monta o diretório atual em `/app`.

- **extras/README.md:** Novo README na pasta extras com exemplo de filtro de logging para não registrar requisições GET /health nos access logs do Uvicorn (útil com New Relic).

- **newrelic.ini:** Template de configuração do agente Python do New Relic (license_key, app_name, log, ambientes, etc.) para uso no boilerplate.

- **docs/commits_2025-03-09.md:** Registro dos commits do dia com tabela e resumo das mudanças.
